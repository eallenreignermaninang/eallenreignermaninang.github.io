name: CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Validate Project
        run: echo "Project looks good"

      - name: CI Success
        run: echo "Pipeline executed successfully"
